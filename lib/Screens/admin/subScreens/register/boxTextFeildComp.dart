import 'package:flutter/material.dart';

class BoxTextFeild extends StatelessWidget {
  const BoxTextFeild({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
